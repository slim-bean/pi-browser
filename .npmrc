; pi bundles @earendil-works/* and typebox and aliases them for extensions, so
; the peerDependencies must never be installed into this package's node_modules
; (npm >= 7 would otherwise fetch a second copy of pi on every `pi install`).
omit=peer
